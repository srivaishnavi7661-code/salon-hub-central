import React, { useState } from 'react';
import { 
  LayoutDashboard, Calendar, Users, UserCheck, 
  Scissors, CreditCard, Package, Award, 
  BarChart3, MapPin, ChevronDown, TrendingUp,
  Clock, CheckCircle2, AlertCircle
} from 'lucide-react';

export default function SalonDashboard() {
  const [activeTab, setActiveTab] = useState('Dashboard');

  // Mock Data matching the live UI
  const stats = [
    { title: "TODAY'S REVENUE", value: "₹756", icon: TrendingUp, color: "text-emerald-500", bg: "bg-emerald-50", border: "border-t-emerald-500" },
    { title: "APPOINTMENTS", value: "1", icon: Calendar, color: "text-purple-500", bg: "bg-purple-50", border: "border-t-purple-500" },
    { title: "NEW CLIENTS", value: "5", icon: Users, color: "text-blue-500", bg: "bg-blue-50", border: "border-t-blue-500" },
    { title: "AVG TICKET", value: "₹756", icon: CreditCard, color: "text-amber-500", bg: "bg-amber-50", border: "border-t-amber-500" },
  ];

  const appointments = [
    { time: "3:30 PM", customer: "Aisha Kapoor", service: "Women Haircut & Blowdry", staff: "Priya Sharma", status: "Completed" },
    { time: "5:30 PM", customer: "Riya Gupta", service: "Hair Colour - Global", staff: "Rohit Mehta", status: "Completed" },
    { time: "7:30 PM", customer: "Nisha Verma", service: "Keratin Treatment", staff: "Anjali Nair", status: "Confirmed" },
    { time: "9:30 PM", customer: "Pooja Desai", service: "Gold Facial", staff: "Deepak Verma", status: "Confirmed" },
  ];

  const menuItems = [
    { name: 'Dashboard', icon: LayoutDashboard },
    { name: 'Appointments', icon: Calendar },
    { name: 'Customers', icon: Users },
    { name: 'Staff', icon: UserCheck },
    { name: 'Services', icon: Scissors },
    { name: 'Point of Sale', icon: CreditCard },
    { name: 'Inventory', icon: Package },
    { name: 'Memberships', icon: Award },
    { name: 'Reports', icon: BarChart3 },
    { name: 'Centers', icon: MapPin },
  ];

  return (
    <div className="flex h-screen bg-slate-50 font-sans antialiased text-slate-800">
      
      {/* SIDEBAR */}
      <aside className="w-64 bg-[#043326] text-slate-200 flex flex-col justify-between p-4">
        <div>
          {/* Logo */}
          <div className="flex items-center gap-2 px-2 py-4 border-b border-[#0b4737]">
            <div className="bg-amber-400 text-[#043326] p-1.5 rounded font-bold text-xl">✨</div>
            <span className="font-bold text-xl tracking-wider text-white">GLOSS</span>
          </div>

          {/* Active Center Dropdown */}
          <div className="mt-6 px-2">
            <label className="text-xs text-slate-400 font-semibold tracking-wider block mb-1">ACTIVE CENTER</label>
            <button className="w-full bg-[#0b4737] hover:bg-[#115744] text-sm text-left py-2.5 px-3 rounded-lg flex items-center justify-between transition-colors border border-[#16634e]">
              <span className="flex items-center gap-2"><MapPin size={16} /> Gloss Studio - Bandra</span>
              <ChevronDown size={14} />
            </button>
          </div>

          {/* Navigation Items */}
          <nav className="mt-6 space-y-1">
            {menuItems.map((item) => {
              const Icon = item.icon;
              const isActive = activeTab === item.name;
              return (
                <button
                  key={item.name}
                  onClick={() => setActiveTab(item.name)}
                  className={`w-full flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-all ${
                    isActive 
                      ? 'bg-[#0b4737] text-white border-l-4 border-amber-400 pl-2' 
                      : 'text-slate-400 hover:bg-[#083d2e] hover:text-slate-200'
                  }`}
                >
                  <Icon size={18} />
                  {item.name}
                </button>
              );
            })}
          </nav>
        </div>

        {/* User Profile Footer */}
        <div className="border-t border-[#0b4737] pt-4 flex items-center gap-3 px-2">
          <div className="w-10 h-10 rounded-full bg-amber-400 text-[#043326] font-bold flex items-center justify-center text-lg">
            G
          </div>
          <div>
            <h4 className="text-sm font-semibold text-white">Admin</h4>
            <p className="text-xs text-slate-400">Salon Manager</p>
          </div>
        </div>
      </aside>

      {/* MAIN CONTENT WORKSPACE */}
      <main className="flex-1 flex flex-col overflow-y-auto">
        
        {/* TOP BAR */}
        <header className="bg-white border-b border-slate-200 px-8 py-4 flex justify-between items-center sticky top-0 z-10">
          <div className="flex items-center gap-2">
            <LayoutDashboard className="text-slate-400" size={20} />
            <h1 className="text-lg font-bold text-slate-800">{activeTab}</h1>
          </div>
          <div className="flex items-center gap-3 text-right">
            <div>
              <h4 className="text-sm font-semibold text-slate-700">Admin</h4>
              <p className="text-xs text-slate-400">Salon Manager</p>
            </div>
            <div className="w-9 h-9 rounded-full bg-[#043326] text-white font-bold flex items-center justify-center text-sm">
              G
            </div>
          </div>
        </header>

        {/* DASHBOARD BODY */}
        <div className="p-8 max-w-7xl w-full mx-auto space-y-6">
          
          {/* Greeting Banner */}
          <div className="bg-white rounded-2xl p-6 border border-slate-200/80 shadow-sm flex items-center justify-between relative overflow-hidden">
            <div className="z-10">
              <h2 className="text-2xl font-bold text-slate-800 flex items-center gap-2">
                Good evening <span className="animate-pulse">✨</span>
              </h2>
              <p className="text-slate-500 text-sm mt-1">
                {new Date().toLocaleDateString('en-IN', { weekday: 'long', day: 'numeric', month: 'short', year: 'numeric' })} · Your salon is thriving today.
              </p>
            </div>
            <div className="text-sm font-semibold tracking-wider text-slate-300 pointer-events-none uppercase select-none">
              GLOSS STUDIO
            </div>
          </div>

          {/* STATS GRID */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
            {stats.map((stat, i) => {
              const Icon = stat.icon;
              return (
                <div key={i} className={`bg-white rounded-xl p-5 border-t-4 ${stat.border} border-x border-b border-slate-200 shadow-sm flex justify-between items-start`}>
                  <div>
                    <span className="text-xs font-bold text-slate-400 tracking-wider block mb-1">{stat.title}</span>
                    <span className="text-2xl font-extrabold text-slate-800">{stat.value}</span>
                  </div>
                  <div className={`p-2.5 rounded-lg ${stat.bg} ${stat.color}`}>
                    <Icon size={20} />
                  </div>
                </div>
              );
            })}
          </div>

          {/* TRENDS & SCHEDULE SECTION */}
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
            
            {/* Revenue Trend Chart Mock */}
            <div className="lg:col-span-2 bg-white rounded-xl p-6 border border-slate-200 shadow-sm flex flex-col justify-between min-h-[350px]">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="font-bold text-slate-800">Revenue Trend</h3>
                  <p className="text-xs text-slate-400">Last 30 days</p>
                </div>
                <span className="text-xs font-medium bg-slate-100 text-slate-600 px-2.5 py-1 rounded-md">Daily</span>
              </div>
              
              {/* Simplified SVG Placeholder Graph resembling your data peaks */}
              <div className="w-full h-48 mt-4 relative flex items-end">
                <svg className="w-full h-full" viewBox="0 0 100 40" preserveAspectRatio="none">
                  <path 
                    d="M 0 40 Q 15 40 25 20 T 40 40 Q 55 40 65 25 T 80 40 Q 90 10 100 28" 
                    fill="none" 
                    stroke="#043326" 
                    strokeWidth="2" 
                  />
                </svg>
                <div className="absolute left-0 bottom-0 text-[10px] text-slate-400">₹0k</div>
                <div className="absolute left-0 top-0 text-[10px] text-slate-400">₹10k</div>
              </div>
              
              <div className="flex justify-between text-[10px] text-slate-400 border-t border-slate-100 pt-3 mt-2">
                <span>28 Apr</span>
                <span>10 May</span>
                <span>22 May</span>
                <span>27 May</span>
              </div>
            </div>

            {/* Today's Schedule Timeline */}
            <div className="bg-white rounded-xl p-6 border border-slate-200 shadow-sm flex flex-col">
              <div className="flex justify-between items-center pb-4 border-b border-slate-100">
                <div>
                  <h3 className="font-bold text-slate-800">Today's Schedule</h3>
                  <p className="text-xs text-slate-400">27 May</p>
                </div>
                <span className="text-xs font-bold bg-emerald-50 text-[#043326] px-2.5 py-1 rounded-full">
                  {appointments.length} appts
                </span>
              </div>

              {/* Appointment Flow Timeline */}
              <div className="mt-4 space-y-4 overflow-y-auto max-h-[260px] pr-1">
                {appointments.map((appt, i) => (
                  <div key={i} className="flex gap-4 group items-start">
                    <div className="w-16 flex-shrink-0 text-right">
                      <span className="text-xs font-bold text-slate-700 block">{appt.time.split(' ')[0]}</span>
                      <span className="text-[10px] font-medium text-slate-400 tracking-tight uppercase block">{appt.time.split(' ')[1]}</span>
                    </div>
                    
                    {/* Timeline Tracker Dot */}
                    <div className="relative flex flex-col items-center self-stretch">
                      <div className={`w-3 h-3 rounded-full border-2 ${appt.status === 'Completed' ? 'border-emerald-500 bg-white' : 'border-amber-500 bg-white'} z-10`} />
                      {i !== appointments.length - 1 && <div className="w-0.5 bg-slate-200 flex-1 my-1" />}
                    </div>

                    {/* Booking metadata */}
                    <div className="flex-1 bg-slate-50 group-hover:bg-slate-100/80 p-3 rounded-lg transition-colors border border-slate-100">
                      <div className="flex justify-between items-start">
                        <h4 className="text-xs font-bold text-slate-800">{appt.customer}</h4>
                        <span className={`text-[10px] px-1.5 py-0.5 rounded font-semibold ${
                          appt.status === 'Completed' ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800'
                        }`}>
                          {appt.status}
                        </span>
                      </div>
                      <p className="text-[11px] text-slate-500 mt-0.5">{appt.service}</p>
                      <p className="text-[10px] text-slate-400 mt-1 flex items-center gap-1">
                        <span className="inline-block w-1.5 h-1.5 rounded-full bg-slate-300" /> {appt.staff}
                      </p>
                    </div>
                  </div>
                ))}
              </div>

            </div>

          </div>

        </div>
      </main>
    </div>
  );
}